const swiperHero = new Swiper('.hero__swiper', {
    direction: 'horizontal',
    loop: true,
    effect: 'fade',
    autoplay: {
        delay: 5000,
        disableOnInteraction: false,
      },
  })

  console.log('test')